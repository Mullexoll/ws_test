import 'package:flutter/material.dart';

sealed class AppConstants {
  static const String websparkEndpoint =
      'https://flutter.webspark.dev/flutter/api';
  static const Color startCellColor = Color(0xFF64FFDA);
  static const Color endCellColor = Color(0xFF009688);
  static const Color shortestPathColor = Color(0xFF4CAF50);
}
